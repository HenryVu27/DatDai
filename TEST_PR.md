# Test PR for GitHub MCP + Code Review

This file is used to test the GitHub MCP integration and Claude code review command.

## Purpose

- Verify GitHub MCP tools work correctly
- Test the `/code-review` slash command

## Test Cases

1. Creating a branch via CLI
2. Opening a PR via `gh pr create`
3. Running code review with Claude
